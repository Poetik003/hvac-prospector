export default async request=>{if(request.method!=='POST')return new Response('Method not allowed',{status:405});const key=Netlify.env.get('BLAND_API_KEY');if(!key)return new Response('BLAND_API_KEY is not configured',{status:503});const b=await request.json().catch(()=>({}));if(!/^\+[1-9]\d{7,14}$/.test(String(b.phoneNumber||'')))return new Response('phoneNumber must be E.164 format',{status:400});const r=await fetch('https://api.bland.ai/v1/calls',{method:'POST',headers:{Authorization:key,'Content-Type':'application/json'},body:JSON.stringify({phone_number:b.phoneNumber,task:String(b.task||'You are a professional HVAC appointment setter. Be transparent, concise, and respectful. Qualify interest and ask permission before scheduling.'),model:'turbo',language:'en-US',max_duration:10,record:false,metadata:{source:'prospectorproai',actor_id:b.actorId||'unknown'}})});return new Response(await r.text(),{status:r.status,headers:{'Content-Type':'application/json'}})};
export const config={path:'/api/place-call'};
